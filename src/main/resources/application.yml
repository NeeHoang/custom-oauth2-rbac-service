spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_db
    username: auth_LeVietHoang
    password: auth_13022004

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      host: localhost
      port: 6379

  security:
    user:
      name: leviethoang
      password: 504901

---
spring:
  config:
    import: classpath:application-secret.yml

---
spring:
  config:
    import: classpath:application-oauth2.yml

#---
#spring:
#  application:
#    name: authentication
#  profiles:
#    active: dev
#
#---
#spring:
#  config:
#    activate:
#      on-profile: dev

#openapi:
#  service:
#    api-docs: api-service
#    server: http://localhost:8080
#    title: API service
#    version: 1.0.0

